<%= form_for(@biztag) do |f| %>
  <% if @biztag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@biztag.errors.count, "error") %> prohibited this biztag from being saved:</h2>
      <ul>
        <% @biztag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>