<%= form_for(@borough) do |f| %>
  <% if @borough.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@borough.errors.count, "error") %> prohibited this borough from being saved:</h2>

      <ul>
      <% @borough.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
